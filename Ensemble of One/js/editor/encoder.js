(function () {
    var Encoder = WinJS.Class.define(
        function () {
            /// <summary>Provides tools for encoding the project into a user-playable media file.</summary>
            //Constructor
        },
        {
            //Instance members
        },
        {
            //Static members
        }
    );

    WinJS.Namespace.define("Ensemble.Editor", {
        Encoder: Encoder
    });
})();