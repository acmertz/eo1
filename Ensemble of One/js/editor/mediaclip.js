(function () {
    var MediaClip = WinJS.Class.define(
        function () {
            /// <summary>Manages a single media clip within a project.</summary>
            //Constructor
        },
        {
            //Instance members
        },
        {
            //Static members
        }
    );

    WinJS.Namespace.define("Ensemble.Editor", {
        MediaClip: MediaClip
    });
})();