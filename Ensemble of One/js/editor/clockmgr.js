(function () {
    var ClockMGR = WinJS.Class.define(
        function () {
            /// <summary>Responsible for tracking and managing time-based events as they occur during playback.</summary>
            //Constructor
        },
        {
            //Instance members
        },
        {
            //Static members
        }
    );

    WinJS.Namespace.define("Ensemble.Editor", {
        ClockMGR: ClockMGR
    });
})();