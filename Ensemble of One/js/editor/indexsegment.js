(function () {
    var IndexSegment = WinJS.Class.define(
        function () {
            /// <summary>Represents a single time section within a project during which only one unique combination of events occurs (i.e., the same clips are playing for the entire segment)</summary>
            //Constructor
        },
        {
            //Instance members
        },
        {
            //Static members
        }
    );

    WinJS.Namespace.define("Ensemble.Editor", {
        IndexSegment: IndexSegment
    });
})();